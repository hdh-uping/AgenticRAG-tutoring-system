# ## 3.2.2 括号匹配的检验 › ## 3.2.3 简单表达式求值

ckEmpty(S)) return FALSE;
    else 
    { Pop(S,ch); if (ch != '(') return FALSE; }
    break;
    case ' '}':
    if (StackEmpty(S)) return FALSE;
    else 
    { Pop(S,ch); if (ch != '[') return FALSE; }
    break;
    case '}':
    if (StackEmpty(S)) return FALSE;
    else
```