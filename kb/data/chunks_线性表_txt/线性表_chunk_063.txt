# ## 5. 求单链表的表长 › ## 6. 查找操作 › ## 1) 按序号查找

设计求单链表表长的算法。需要注意的是，头节点不在链表长度统计范围之内。

算法 2.10 求带头节点的表长  
```c
int Length_LinkList1(LinkList L)    /*当链表带头节点时*/
{    LNode *p=L;    /*p指向头节点*/
    int i=0;    /*设计数器变量i*/
    while(p->next)
    {    p=p->next;i++; }    /*p指针依次后移*/
    return i;
}
```