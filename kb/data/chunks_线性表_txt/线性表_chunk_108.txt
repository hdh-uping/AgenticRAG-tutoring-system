# ## 1. 实现顺序表常用操作的代码 › ## 2. 顺序表常用操作的程序运行结果 › ## 2.4.2 单链表的常用操作

```c
{
    int i,n;
    printf("请输入该线性表的元素个数：n=");
    scanf("%d",&n);
    while(n>L->listsize)
    {
    printf("超出了线性表的存储空间，请重新输入：\n");
    scanf("%d",&n);
    }
    L->length=n;
    printf("请依次输入该线性表各元素的值：\n");
    for(i=0;i<n;i++) scanf("%d",&L->elem[i]);
}
/*【算法2.2 顺序表的插入】*/
/*在顺序表L的第i个位置之前插入新的元素e，若成功则返回1，否则返回-1*/